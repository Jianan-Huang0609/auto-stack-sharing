# 个人 AI 自动化的分层体系
## n8n × Skill × Agent：怎么分工、怎么偷懒、怎么结合

> 分享人：Jianan
> 适用场景：希望搭建个人 AI 自动化体系，但面对 n8n / Dify / Claude Code / Agent 框架不知从何下手的同学

---

## 一、先抛观点：不要把所有事都塞进一个工具

过去半年，大家对 AI 自动化的理解经历了三波迭代：

- **第一波**：什么都用 LLM 单次对话解决 → 不够自动化
- **第二波**：什么都塞进 n8n/Dify → 流程图变成意大利面
- **第三波（现在）**：**按"确定性"分层，不同工具做不同的事**

**核心判断公式**：

| 问题 | 回答 | 用哪层 |
|------|------|--------|
| 路径是否预先可画？ | 是 | **n8n / Dify** |
| 是否是单次高质量输出？ | 是 | **Skill** |
| 是否需要运行时决策？ | 是 | **Agent** |

---

## 二、三层分工：各自解决什么问题

### 第一层：n8n / Dify —— 确定性流程的"管道工"

**本质**：事件触发 + 固定路径。流程图画完就定了。

**适合做**：
- 定时抓取（cron 触发 YouTube/X/小红书）
- Webhook 响应（收到邮件 → 自动处理）
- 跨系统同步（Airtable ↔ Feishu ↔ Discord）
- 通知分发（消息路由到不同渠道）

**不适合做**：
- 需要"思考"下一步该做什么的场景
- 需要高审美/高创作质量的单次输出
- 复杂的多轮对话决策

---

### 第二层：Skill —— 一次性高质量输出的"工匠"

**本质**：把某类任务的最佳实践封装成上下文，让模型每次调用都能调出"专家模式"。

**适合做**：
- 做 PPT（视觉设计系统 + 内容逻辑 + 口述脚本）
- 做视频风格化处理（运镜参数 + 风格 prompt）
- 做高审美的单次交付（海报、文案、代码模板）

**关键特征**：任务边界清晰、单次可完成、需要"领域专家级"的 know-how。

---

### 第三层：Agent —— 不确定性决策的"执行者"

**本质**：每一步根据上一步结果决定下一步做什么。**"图一边跑一边画"**。

**适合做**：
- 写深度文稿（需要在过程中决定查什么资料、用什么结构）
- 复杂任务分解（先规划、再执行、再评估）
- 多工具协同（agent 决定什么时候调 skill、什么时候调 n8n）

**关键区别**：
- n8n 是**静态多步**（路径预先画好）
- agent 是**动态多步**（运行时决策）
- **"多步"本身不是 agent 的判据，"运行时决策"才是**

---

## 三、三者如何"偷懒"：让系统替你工作

偷懒的本质是**把人类的工作转嫁给系统本身**。

### n8n / Dify 的偷懒路径

| 层级 | 做法 | 效果 |
|------|------|------|
| 初级 | 用 AI Agent 节点替代 IF/Switch 判断 | 不用写复杂条件分支 |
| 中级 | **Vibe 描述流程** → LLM 生成 workflow JSON | 自然语言即配置 |
| 高级 | 运行日志回流给 LLM，自动优化节点 | 流程自我进化 |

### Skill 的偷懒路径

| 层级 | 做法 | 效果 |
|------|------|------|
| 初级 | 每次任务完成后让模型反思并 append 到 SKILL.md | 自总结 |
| 中级 | Meta-agent 定期审视所有 skill，合并/拆分/删除 | 自进化 |
| 高级 | 从成熟 skill 派生变体（同 80% 逻辑，不同口吻/场景） | 模板派生 |

### Agent 的偷懒路径

| 层级 | 做法 | 效果 |
|------|------|------|
| 初级 | 直接用 LangGraph / CrewAI / Claude Agent SDK | 框架已备齐基础设施 |
| 中级 | Agent 自己决定调哪个 skill、触发哪个 n8n workflow | 自我调度 |
| 高级 | 加一个 evaluator agent 对输出打分，不合格自动重试 | 自我评估 |

---

## 四、三个有机结合的案例

### 案例 1：做 PPT —— Skill 主导

**为什么 Skill 是主角**：做一份好 PPT 本质是"单次高密度输出"，需要视觉设计 + 内容逻辑 + 口述脚本的领域知识沉淀。

**协作方式**：
1. **Skill（主力）**：`jianan-presentation-system` 生成视觉 + 内容 + 脚本
2. **Agent（辅助）**：生成前先做"需求澄清对话"，决定章节结构
3. **n8n（辅助）**：生成后自动推送到飞书 + 发邮件给 reviewer

---

### 案例 2：做内容文稿 —— Agent 主导

**为什么 Agent 是主角**：写深度文稿需要多轮决策——查什么资料、用什么结构、引用哪些案例、哪里加原创观点。路径无法预先画好。

**协作方式**：
1. **Agent（主力）**：接到题目 → 规划大纲 → 过程中动态决策
2. **Skill（借力）**：调用写作 skill 做结构化写作和润色
3. **n8n（借力）**：发现某段需要数据支持时，触发抓取资料的 workflow

---

### 案例 3：做视频 idea 抓取到生成 —— 三者深度协作

**为什么是压轴**：这个案例三层全用上，每层分工特别清晰，能完整体现"分层自动化"的威力。

**完整闭环**：

| 阶段 | 主导层 | 做什么 |
|------|--------|--------|
| 1. idea 抓取 | **n8n** | cron 定时抓 YouTube/X/小红书热门 AI 内容 → 写入 Airtable |
| 2. idea 评估与包装 | **Agent** | 判断哪些 idea 值得做 → 生成脚本大纲 → 决定视觉风格 |
| 3. 视频生成 | **Skill + API** | 风格化 prompt 模板调 Kling/Runway API → 本地拼接 |
| 4. 分发 | **n8n** | 上传各平台 + 发通知到 Discord/飞书 |

**核心亮点**：**n8n 起手、Agent 决策、Skill 执行、n8n 收尾**的完整闭环。

---

## 五、升华：这是一个"分层自动化个人操作系统"

三者不是三个独立工具，而是**一套分层协作的基础设施**：

- **n8n 是"神经系统"**——传递信号、触发动作
- **Skill 是"肌肉记忆"**——固化专业技能
- **Agent 是"大脑"**——做判断、做决策、做调度

在 AI 时代，**每个人都应该构建自己的这套基础设施**。不是因为酷，而是因为：

1. **认知杠杆**：一次设计，长期复用
2. **能力沉淀**：让经验不随项目结束而消散
3. **注意力保护**：把重复性工作交给系统，把创造性工作留给自己

---

## 六、落地建议

**从哪开始？**

1. **先跑通一条 n8n workflow**（比如定时抓取一个信息源）——建立"流程即代码"的直觉
2. **再封装一个 skill**（比如你最常做的报告模板）——体验"领域知识沉淀"的价值
3. **最后引入 agent**（让它调度前两者）——感受"动态决策"的魔力

**不要追求一步到位，按这个顺序走，每一步都有即时收益。**

---

*分享素材 / 问题讨论 欢迎在飞书群继续*
